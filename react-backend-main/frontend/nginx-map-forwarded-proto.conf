# Cloudflare (and other edges) send the visitor's scheme here; inner nginx often sees $scheme as http.
map $http_x_forwarded_proto $forwarded_proto {
    ''      $scheme;
    default $http_x_forwarded_proto;
}
